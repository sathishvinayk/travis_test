language: node_js
sudo: false
dist: trusty

jobs:
  include:
    - stage: Build
      install:
        npm install
  include:
    - stage: Test
      install:
        npm test
  include:
    - stage: Container
      script: build.sh container